// Tasbal Widget Test
//
// アプリケーションの基本的な動作を検証するテスト

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts successfully', (WidgetTester tester) async {
    // TODO: TasbalAppの初期化テストを実装
    // 現在はスキップ（DI初期化が必要なため）
  });
}
